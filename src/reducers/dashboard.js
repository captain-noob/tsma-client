import ip from "icepick"
//import { services, CONSTANTS } from "../utils"

const initialState = ip.freeze({})

export default function dashboard(state = initialState, action) {
    switch (action.type) {
    default: {
        return state
    }
    }
}
